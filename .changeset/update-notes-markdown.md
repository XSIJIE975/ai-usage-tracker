---
"ai-usage-tracker": minor
---

检查更新的更新说明支持 Markdown 渲染，标题、列表、加粗、代码、链接、表格等格式正确显示。

### 检查更新

- **更新说明 Markdown 渲染**：设置 → 检查更新里的更新说明改为按 Markdown 排版，标题层级、列表、加粗、行内代码与代码块、链接、图片、表格、引用均可正常显示；说明过长时该区域可滚动
- **链接在浏览器打开**：说明里的链接点击后在系统默认浏览器中打开，不会把应用界面导航走
- **修复**：说明里的小节标题原先会露出字面 `###`，现已正常显示为标题
