# Glossary

## 供应商

程序内置的供应商种类（目前五个：OpenCode Go、DeepSeek、智谱 GLM、腾讯 WorkBuddy / CodeBuddy、Qoder），定义了取数方式、凭据槽位与统计形态。同一种类可以添加多个实例分别追踪。
_Avoid_：账户、配置（这两个词指「供应商实例」，见下）

## 供应商实例

某个供应商的一份可独立追踪的配置，自带凭据、备注、阈值、自动刷新开关、统计与告警；同一供应商可以有多个实例（如两个 DeepSeek 账号）。卡片、快照、通知与刷新状态都以实例为单位。
_Avoid_：账户、配置、条目

## 站点（多站供应商）

同一供应商下互不相通的登录域（Qoder 的 qoder.com.cn / qoder.com，WorkBuddy 的 workbuddy.cn / workbuddy.ai）。站点是**实例**的显式属性（`site`，缺省中国站），不是新的供应商种类：它决定端点域名与 Origin/Referer、决定凭据从哪个域粘贴、也决定这一站有没有某个功能面（按站能力表）。哪些种类多站的唯一判据是 `providerSites`，弹窗的「站点」下拉与卡片标题旁的站点徽标都问它。换站不自动清凭据——跨登录域必然失效，得重贴该站的凭据。机制见 ADR-0031。
_Avoid_：区域、realm、域名（这些词都指站点）；把两个站当同一账号；从粘贴内容猜站

## 备注

用户为实例写的显示名，卡片主标题与告警标题优先使用它；留空时回退供应商名。

## 置顶

把实例固定在网格最前的操作，主窗口与快速面板共用同一顺序（置顶优先，其余按拖拽顺序）。

## 快照历史

刷新时对实例用量的一次采样所积累的时间序列（保留 30 天）。只在程序运行且刷新时产生，因此是稀疏采样而非账单；当前无界面消费方，仅持续积累。

## UserToken

DeepSeek 开放平台网页（platform.deepseek.com）登录后保存在浏览器 localStorage 中的登录态令牌，用于调用平台侧用量接口（`Authorization: Bearer <token>`）。与 DeepSeek API Key（`sk-...`，用于官方推理 API 与余额查询）是两种不同凭据。

## DeepSeek API Key

`sk-...` 形式的密钥，用于调用 DeepSeek 官方推理 API 与余额查询接口。与 UserToken 是两种不同凭据：API Key 授权程序访问账号的推理能力，UserToken 代表用户本人对开放平台控制台的会话。

## 账户余额

按量付费（预付费）现金账户的可用金额，单位元；DeepSeek 与智谱 GLM 各自的余额接口返回，展示为卡片上的文本行。与 Coding Plan 配额（订阅制请求点数）是两种量纲，同一张智谱卡片上并存、互不折算。智谱实例可另设余额告警阈值（元），与配额百分比阈值相互独立。
_Avoid_：余额（单独说「余额」时分不清指账户余额还是配额余量，说清楚是哪个）

## OpenCode Go

第二个受支持的供应商（provider 标识 `opencode-go`），基于 opencode.ai 的用量服务。凭据为 Workspace ID 与 Auth Cookie（两者必填），可选配 API Key 供官方 usage 接口上线后使用。设置页中与 DeepSeek 并列的供应商页签即指它。

## 智谱 GLM

第三个受支持的供应商（provider 标识 `glm`），同时追踪智谱 bigmodel.cn 的两样东西：Coding Plan 订阅配额与账户余额（按量付费现金账户）。两者数据都来自控制台私有接口（见 ADR-0009/0010/0013），凭据仅需一枚 Coding Plan API Key。

## Coding Plan API Key

智谱控制台 Coding Plan 页「生成 API Key」所得的密钥（长期有效），即 Claude Code 等 Anthropic 兼容客户端里配置的 `ANTHROPIC_AUTH_TOKEN`。用于配额、用量统计与账户余额查询（Bearer 注入，官方 glm-plan-usage 插件同款用法，余额通道见 ADR-0013）。

## Coding Plan

智谱的订阅制编码套餐，分 Lite / Pro / Max 档（快照上以小写 `level` 显示，如 `lite`）。额度机制为「5 小时窗口 + 周配额」双窗口的请求点数（接口类型 `CREDIT_LIMIT`）。与账户余额是两种量纲：套餐消耗请求点数，不扣现金。

## 未订阅 Coding Plan

智谱账号没有有效 Coding Plan 订阅的正常状态，不是错误：配额与用量接口（monitor 族）对这类账号返回「当前用户不存在coding plan」的错误封套，快照把它映射为一条中性事实行（订阅账号该位置是「套餐档位」徽章行）；账户余额与重置卡（biz 族接口）不依赖订阅，照常查询。未订阅实例没有配额窗口，告警指标回退为账户余额数值（与 DeepSeek 同机制），也不参与托盘用量环。
_Avoid_：未开通（服务端旧措辞，与凭据无效的关键词重叠，易误分类）

## 5 小时窗口

Coding Plan 的滚动 5 小时请求配额窗口（接口字段 `unit=3, number=5`）：任意时刻的配额按最近 5 小时内的消耗计算，窗口内点数用尽需等最早的消耗滑出窗口。与 OpenCode Go 的 5 小时额度同义。

## 周配额

Coding Plan 的滚动 7 天请求配额窗口（接口字段 `unit=6, number=1`）：重置时间由服务端下发（`nextResetTime`），不是自然周一。快照主指标与告警阈值取该窗口的已用百分比——它是重置周期最长的窗口，代表最紧的约束。

## 模型用量

Coding Plan 的按模型 Token 消耗统计（`model-usage` 端点，列式结构按时间桶对齐）。接口只提供按模型的 Token 序列；请求次数仅有全模型合计，无费用数据。统计页「智谱 GLM」页签的 Token 趋势图与模型明细即来自它。

## 工具用量

Coding Plan 的工具调用统计（`tool-usage` 端点），只提供**调用次数**（官网展示的积分消耗是前端按单价折算的，单价不在接口响应中）。展示序列以服务端下发的动态工具列表为权威（自带中英文名、只含有调用的工具，如「联网搜索 MCP」「网页读取 MCP」，字段 2026-09-04 实测确认）；固定三序列（联网搜索、网页阅读（MCP）、Zread（MCP））是同一批数据的旧形态别名，仅在动态列表为空时兜底。快照卡片不含它，仅在统计页展示。

## 重置卡

智谱赠送的额度恢复卡（官网「用量重置额度」），每张可立即恢复对应窗口的额度一次，独立有效期、过期自动失效；使用周卡会同步重置 5 小时额度且不额外消耗 5 小时卡。数据来自控制台私有接口（见 ADR-0014），按可用张数展示为卡片文本行（无可用卡时不显示）；统计抽屉可查看明细并直接使用（不可逆，二次确认）。读取与使用均为 Coding Plan API Key 鉴权，使用接口规约来自官网前端源码静态分析。

**到账**指账号出现此前未见过的可用卡（按服务端 recordId 区分，同一张卡是哪一个由它唯一决定），刷新数据后发现到账即发一条系统提醒；卡被使用或过期不算到账，同一张卡只提醒一次。仅可用卡参与「新到账」判定；首刷时账号里已有的卡是基线，不回放提醒。
_Avoid_：重置额度（与「配额窗口自动重置」易混，说「重置卡」指卡，说「窗口重置」指自动滚动恢复）；「到账」也不等于「窗口重置」——前者是拿到新卡，后者是额度自动滚动恢复

## 系统健康度

控制台「系统健康度」的平台侧指标（`model-performance-day` 端点，按日粒度）：Max&Pro 与 Lite 档的高峰期平均 Decode 速度（tokens/s）与请求成功率。它反映智谱平台整体服务质量，与具体实例无关；统计页展示 Decode 速度双折线，跟随时间范围，取不到数据时整块隐藏。接口属 monitor 族私有接口（ADR-0009 同族），Coding Plan API Key 鉴权。

## 腾讯 WorkBuddy / CodeBuddy

第四个受支持的供应商（provider 标识 `workbuddy`），追踪腾讯 WorkBuddy 的积分余量、套餐明细、签到、连登、喵喵旅行与消耗明细（统计抽屉）。WorkBuddy 与 CodeBuddy 是同一账号积分体系下的两个产品，共用一份账务，故不拆成两个供应商。数据来自 workbuddy.cn 的 web 接口（非公开，凭 WorkBuddy 登录凭据调用，接入边界见 ADR-0029）。国际站（workbuddy.ai）是同一供应商的另一个站点（ADR-0031）：2026-09-22 真机确认该站没有国区这套成长运营（签到、连登、喵喵旅行都不存在），用量页与消耗明细与中国站同款；响应结构同构，请求体只发公共骨架（目录码不参与结果）。
_Avoid_：把 CodeBuddy 当成另一个供应商（同一积分体系）；「CodeBuddy IDE 的编程对话」与本工具无关，本工具只读它的积分账务

## WorkBuddy 登录凭据

workbuddy.cn 网页端的登录态：`session` + `session_2` 两个服务端会话 Cookie，**加上登录时那一条浏览器 UA**——网关按这三元组同源校验，只发 session、或 UA 与登录时差一个字符，都是 401（2026-09-21 同 Cookie 二分实测）。三项各占一个凭据槽、各存用户填的原文（`session` / `session2` / `userAgent`），Cookie 头与 UA 的拼装和字符集白名单统一在 Rust 端 `instances::workbuddy_session`（探测与刷新共用同一实现），前端不改写用户输入也不参与拼头。UA 没有兜底值：这一格必填，缺了直接报错——历史上那个内置 Edge 常量只对"登录浏览器恰好是它"的人生效，浏览器升版就 401 且无从解释。0.9.x 那种「Copy as cURL 整串」的录法与其解析器已退役，历史凭据刻意不做迁移——升级后三格为空，重填即恢复。退出登录、换浏览器或浏览器升版后重填一次即恢复。网页端**没有** Bearer token——Bearer JWT 只存在于官方 IDE 扩展/CLI 的 OAuth 链路（与网页会话是两条通道，社区工具走的正是后者）；不做自动刷新：与官方客户端并存时主动续期会互相踢下线（ADR-0029）。与 DeepSeek UserToken 同属「网页登录态」类凭据，存于凭据库。
_Avoid_：只叫它「登录 Cookie」（不完整，UA 是同一条凭据的一部分）；「复制 session 的值」（这正是 401 的成因）

## 积分

WorkBuddy 订阅的容量单位，按套餐发放、随套餐到期作废；卡片主行展示总余量与总量。与「账户余额」（现金，元）是两种量纲：workbuddy 实例没有现金余额，速览面板的余额位对它显示积分。
_Avoid_：余额（单独说「余额」在 workbuddy 语境指积分余量，跨供应商场合请说「积分」）

## 套餐

积分的发放批次：每份套餐有余量、总量与到期时间，到期未用完的积分作废。同一套餐会按周期重复发放（接口返回多条同名 Account），卡片上同名合并为一行、到期时间取组内最早，按最早到期升序排列；告警阈值的已用百分比按全部有效套餐聚合计算。

## 签到

WorkBuddy 的每日积分福利（幂等活动接口，当日已签有专门业务码）：实例每天第一次刷新（自动或手动均计）时代用户执行一次，先签到后取数，签到到账的积分当轮即可见；成功发一条系统通知，已签静默。不设独立开关——签到完全跟随刷新行为，不刷新就不会签（ADR-0029）。
_Avoid_：自动签到（它没有独立开关，是刷新链的一环）

## 连登

连续签到天数（只读），每次刷新并行获取，卡片以文本行展示。补签卡、礼包领取等其余活动接口不接入（ADR-0029）。

## 喵喵旅行

WorkBuddy 的挂机赚积分活动：喵喵被派出旅行（数小时），到站后可领奖励积分。每次刷新跟随状态机处理——到站自动领奖（成功发一条系统通知，同一行程只通知一次），空闲且当日名额未用尽时自动再出发（名额由服务端按自然日控制，工具不再加本地开关）。领奖与出发都是非消耗写操作（积分只进不出，ADR-0029 修订纳入白名单）。旅行中卡片显示目的地与预计回来时刻，到站/空闲不出行。
_Avoid_：自动领奖（它没有独立开关，是刷新链的一环）

## 消耗明细

WorkBuddy 的逐条请求积分记录（官网控制台同款数据，只读接口）：统计抽屉里展示模型维度表（请求次数、积分消耗、单次均耗、合计）、每日趋势（积分/次数切换）、用途分布（对话/提示词增强/子代理等）与逐条明细（时间、摘要、积分、模型、用途）。时间范围最长 30 天（官方接口限制）；摘要用官网截断版原文，不取请求全文。0 积分的请求是官方内部模型的调用（会话摘要等），如实计入次数。
_Avoid_：用量（WorkBuddy 没有 token 口径的用量，只有积分消耗）；导出（官网已提供，二期再说）

## Qoder

第五个受支持的供应商（provider 标识 `qoder`），追踪阿里 Qoder 的大模型积分余量。数据来自账号控制台的只读私有接口 `GET /api/v2/me/usages/big_model_credits`（凭 CodexBar 先例接入，见 ADR-0030），凭据是网页会话 Cookie 的**值**——只取 `qoder_session_cookie` 这一个键，键名与 `Cookie:` 头由后端拼（ADR-0030 二次修订）。站点（国际站 qoder.com / 中国站 qoder.com.cn）是实例的显式属性，两套登录域 Cookie 不互通。仍然只读、无任何动作接口：卡片读汇总端点里的四个配额容器出行（见「Qoder 积分」）；统计抽屉另读逐条明细与近一年分布两个端点（契约与口径见 ADR-0030 §6），两站同契约、免费号也有完整历史，故两站都出「查看统计」。官网确有「每日签到送 100 积分」活动，而领取入口只在 Qoder 桌面端浮窗——本工具不接（限时活动，判据要写死活动额度与有效期两个常量，理由见 ADR-0030 Considered Options）。
_Avoid_：把 qoder.com 与 qoder.com.cn 当同一账号（登录域不互通）；官方 API（个人账号没有，Teams/企业版才有，鉴权是 Organization API Key）；贴整段 Cookie 头（凭据槽只放 `qoder_session_cookie` 的值本体）；代领签到（做不到，也不该做）

## Qoder 积分

Qoder 订阅的大模型容量单位（端点名 big_model_credits），按周期发放、重置时间由服务端 `nextResetAt` 下发。一个有分配的配额容器一行进度，行名「订阅积分 / 资源包积分 / 专属资源包」（对齐官网 CLI 的 Plan Credits 与 Add-on Credits 两栏），恒零容器不出行；主指标是订阅行（全响应唯一的 `nextResetAt` 只挂它，资源包行刻意不带重置时刻，阈值告警才不盯错窗）。资源包各笔有自己的 `expires_at`，所以另出一条明细文本行报「余若干 · 最早某天到期」，到期只算还有余量的那些笔——已耗尽那笔的到期日不代表余量的去处。已用百分比一律本地按 used/total 算，接口下发的 `usage_percentage` 不采用（真机证实它是 0~100 的向上取整整数，拿它等于用站点替你进位过的数字）。速览的余额位承载**实例级**总余量（`total_quota` 汇总位），挂在第一条进度行上，不是那一行自己的余量。总量为 0 表示这个套餐根本没分配积分（体验版，官网自己也写「0 / 0（已使用 0%）」），卡片出「未分配积分」一行，不出百分比也不出重置倒计时，速览面板同说（不再报「暂无数据」）。重置时刻只认未来的：`nextResetAt` 已过期（未分配积分账号的周期是冻结的，样本里停在八个月前）与缺失同处理，宁可不显示也不挂一个过去的「重置」。与 WorkBuddy 的「积分」是两家产品各自的容量单位，量纲同构（按期重置的配额）但互不相通，跨供应商场合先说清是哪家的积分。
_Avoid_：金额（Qoder 积分不是现金账户，没有金额量纲，不参与 DeepSeek 那套余额回退）；Credits / 大模型积分（中文界面统一叫「积分」，与 WorkBuddy 积分靠供应商上下文区分）；把零总量当「已用 100%」（那是未分配，不是用满）；把订阅与资源包的用量加成「总共用了百分之几」（两种不同周期的额度相加，正是拆分前吞掉「订阅见底」信号的那个口径）；用 `resetsAt` 承载资源包到期（卡片那一格写死是「重置」）。速览面板的「账户余额」位与 WorkBuddy 同例承载积分余量数值，是既有展示位而非金额量纲

## 快速面板

常驻后台的独立桌面小窗（代码中为 QuickWindow，窗口标识 `quick`）：全局快捷键或托盘右键菜单「显示快速面板」唤起，原位显示（保持上次拖放位置），失焦自动隐藏，集中展示各实例用量概要、告警与通知中心。顶栏双击打开主窗口并收起面板（单击并拖动顶栏移动面板）；高度随内容自适应（下限 240px，上限为屏幕工作区的 80%）。卡片顺序与置顶跟随主窗口，但不可拖拽、不含统计入口。与主窗口共用同一前端界面代码，是主窗口之外两个独立窗口之一（另一个是速览面板）——主题、语言等纯界面偏好需要跨窗口保持一致。

## 速览面板

托盘左键单击唤起的独立小窗（窗口标识 `glance`）：锚定在托盘图标旁，再点一次或失焦自动收起（固定行为，无开关；Linux 取不到可靠坐标时回退上次位置）。全新布局、不与快速面板共用卡片组件，内置两种展示形态——紧凑行列表与迷你卡片环——面板内切换并记忆。设置页只控制展示实例范围；字段（余额、重置倒计时、多窗口明细、告警标记）与底部通知条固定全量展示。与主窗口共用同一前端界面代码。
_Avoid_：托盘面板、迷你面板（统一叫「速览面板」）；勿与「快速面板」混用——那是快捷键唤起的另一扇窗

## 主指标

从实例快照中抽取的单一数值，告警阈值以它为准：快照含配额进度行时，取重置周期最长一行的已用百分比（智谱 GLM 为周配额，OpenCode Go 为重置最远的窗口）；没有进度行的实例（DeepSeek）取账户余额数值。两种量纲并存——百分比（配额）与金额（余额），互不折算。托盘用量环不用它——环展示各配额窗口自身，数字标注最紧窗口。

## 额度耗尽

任一配额窗口的已用百分比达到 100%：该窗口在重置前不可用。无论实例是否设置阈值都触发告警——主指标阈值告警只盯重置周期最长的预算窗、刻意放行短周期窗的日常冲高，撞满意味着已经无额度可用，是对它的必要补齐。
_Avoid_：配额用完（与余额不足的金额量纲混淆）、超限告警（阈值告警的语义是接近预警，不是耗尽）

## 最紧实例

主指标为百分比量纲的实例中，已用百分比最高者。DeepSeek 实例的主指标是账户余额（金额量纲），不参与最紧比较，仅通过活跃告警影响用量环颜色。

## 钉选实例

用户在设置中指定用量环固定展示的供应商实例，覆盖「最紧实例」默认行为；选「自动」即恢复默认。

## 托盘图标方案

托盘图标的呈现方案：设置页选择、切换即生效，架构上可扩展更多形态。本期两套——「默认」：静态应用图标、告警时换警示图（现状）；「用量环」：见下。
_Avoid_：托盘皮肤、图标主题（不涉及配色系统）

## 用量环

托盘图标方案之一：多层环形进度展示单个实例的若干配额窗口——按重置周期短→长由外到内嵌套、至多三扇（仅百分比量纲可绘制），按固定档位变色，与用户自设的告警阈值无关；任一实例处于活跃告警时强制红色；macOS 图标旁数字标注最紧窗口的已用百分比。默认展示最紧实例，可被钉选实例覆盖；无可绘数据（凭据库锁定、尚无快照、或所选实例为金额量纲）时回退默认图标。与托盘悬停提示一起构成托盘速览面。
_Avoid_：托盘进度条、图标仪表盘、任务栏缩略图（后者是另一表面——主窗口任务栏按钮的悬停预览，与托盘无关）

## 跨窗口广播

程序状态变化（设置、实例、告警、刷新完成、主题、凭据库）以广播事件同步到全部窗口：设置与实例以主窗口为唯一写入口，窗口只订阅不各自持久化。任何窗口展示的都是同一份状态，配置变更即时生效、无需手动刷新；新窗口接入即自动跟随。广播传播的是**状态**，不是**刷新的产物**——后者见「快照收敛」。
_Avoid_：窗口间消息、窗口同步刷新（「刷新」指供应商数据拉取，与状态广播无关）

## 快照收敛

刷新产物（快照）向所有窗口收敛的过程：任一窗口完成一次抓取并落库后，其余窗口都以最新落库快照重新求值自己的展示。托盘数字与各面板展示的都是同一份最新快照的投影，因此「谁刷的」不影响「谁看到的」。
_Avoid_：窗口同步刷新、数据同步（不指明同步什么时不用这个词）

## 托盘写入方

推开托盘呈现的那一个窗口（主窗口），是改动托盘数字的唯一路径。面板不写托盘，只写快照事实源；面板刷新要让数字变化，走的是快照收敛。
_Avoid_：托盘数据源（混淆「谁写」与「写什么」——托盘的数据来源是快照事实源）

## 快照事实源

落库的最新快照（数据库），跨窗口展示的唯一权威；各窗口内存里的快照只是它的投影，不构成事实。既含成功采样、也含失败采样（错误快照）——「没有数据」与「有旧数据」是两种事实，事实源如实区分。
_Avoid_：实时数据、内存快照

## 错误快照

刷新失败时落库的采样（`status=error`，无用量行），是快照事实源的正式成员。展示层对它诚实显示错误而非旧数据；告警对它是未知而非正常——冻结已激活的告警态（既不触发也不解除），网络恢复后不因边沿重置而重复通知。
_Avoid_：失败记录（它不是日志，是会被各窗口投影的快照）、占位快照

## 告警评估方

唯一执行告警评估的窗口（主窗口）：边沿触发、冷却与通知都只由它驱动。面板窗口不评估告警，只经跨窗口广播同步告警态用于展示；主窗口在快照收敛后以最新落库快照重跑评估，因此「谁刷的」不影响「谁告警」。边沿与冷却状态持久于后端事实源，评估窗口重载或应用重启后原样恢复；重复通知的最终拦截在后端，不由评估方单方面保证。
_Avoid_：告警监听者（评估是主动求值，不是被动监听）

## 冷却

同一规则两次告警通知之间的最短墙钟间隔，全局设置、按小时配置。跨窗口重载与应用重启持续有效；计时基准是上一次实际发出的通知，与前端会话无关。与错误快照的冻结是两回事——冻结是「未知时不表态」，冷却是「已知重复时不打扰」。
_Avoid_：会话冷却（旧语义，随前端重载归零）、告警冻结（那是错误快照的语义）

## 凭据库

Rust 后端管理的加密存储（实现名 Credential Vault），按供应商实例嵌套保存各类凭据（每个实例一份凭据槽位集合），前端不接触明文凭据。密文文件保存在应用数据目录下，用设备密钥加密。

## 设备密钥

随机生成的 256 位主加密密钥，由操作系统钥匙串托管（Windows Credential Manager / macOS Keychain / Linux Secret Service），与本机及系统用户绑定。凭据库文件用它加密，复制到另一台机器无法解密。
_Avoid_：主密钥、机器密钥、机器绑定密钥（它不是从机器派生的，而是随机生成后托管在本机）

## 凭据库迁移

废除主密码后的一次性升级动作：用户最后一次输入旧主密码，程序把旧凭据库解密后用设备密钥重新加密。首次升级启动时引导，跳过后可在设置页补做。

## 自动刷新总开关

设置页「通用」页签中的全局开关，是程序一切自动刷新行为的总闸：总览快照的定时刷新、统计页的定时刷新都由它控制。关闭后程序只进行手动刷新。

## 供应商自动刷新

每个供应商实例配置弹窗中的开关，决定该实例是否参与定时刷新（总览快照与统计抽屉中该实例的数据）。受自动刷新总开关门控：总开关关闭时禁用置灰，开关值保留。不影响手动刷新——手动「刷新」始终拉取所有实例。

## 缓存命中率

统计页展示的派生指标：`PROMPT_CACHE_HIT_TOKEN ÷ (PROMPT_CACHE_HIT_TOKEN + PROMPT_CACHE_MISS_TOKEN)`，即输入 Token 中命中提示缓存的占比。用于解释成本差异——DeepSeek 缓存命中的单价远低于未命中。

## 基础 CI

在 `dev` 分支推送或向 `main` 发起 PR 时运行的轻量检查流程。它只执行前端测试、前端构建和 Rust 测试，不生成安装包。

## Version Packages PR

由 Changesets 自动创建或更新的发布 PR。合并该 PR 会更新版本号、CHANGELOG 和 Tauri 配置，并成为打包流程的触发入口。

## Draft Release

发布 workflow 创建的暂存 GitHub Release。安装包上传完成后仍为草稿，需要用户在 GitHub 网页手动发布。草稿期已包含签名的更新产物与 `latest.json`；手动发布即同时开放安装包下载与自动更新通道。

## 版本源

`package.json` 中的版本号是唯一版本源。发布脚本会在 Changesets 更新版本后同步 `src-tauri/Cargo.toml`、`src-tauri/Cargo.lock` 和 `src-tauri/tauri.conf.json`。

## 更新签名密钥

`tauri signer generate` 生成的 minisign 密钥对。公钥固化在 `tauri.conf.json`，用于校验更新包签名；私钥与密码仅存开发者本机（仓库外 `~/.tauri/`）与 GitHub Secrets（`TAURI_SIGNING_PRIVATE_KEY` / `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`），永不入库。私钥丢失即失去更新通道，存量用户只能手动重装。

## latest.json

应用内自动更新通道的唯一数据源，`releases/latest/download/latest.json` 恒指向最新已发布 Release。多平台构建各自上传更新产物后，由发布流程末尾的 `fill-updater-notes.mjs` 依据 Release 上的签名文件确定性重建（避免并行构建写清单的竞态）。macOS 为 universal 统一构建：更新插件按运行时架构匹配 `darwin-aarch64` / `darwin-x86_64` 键而不认识 `darwin-universal`，因此清单里两种架构的键都指向同一个 universal 产物。

## 自动更新

启动后在后台静默检查 `latest.json`：发现新版本时顶栏出现「新版本」徽标，设置 → 通用的「关于与更新」卡片可手动检查、查看变更说明、下载并安装（Windows 由 NSIS 安装器静默完成，无需管理员权限）。首个带更新能力的版本无法被更早的安装自动升级到，需手动重装一次。

## 变更说明

`latest.json` 的 `notes` 字段，即 GitHub Release 说明在应用内的对应物：发布流程从 `CHANGELOG.md` 的当前版本段落截取，写入清单后由设置 → 检查更新的说明区**按 Markdown 渲染**（见 ADR-0028）。存的是原始 Markdown —— `###`、`**`、链接、缩进嵌套都原样保留，发布侧不做降级；原始 HTML 与 `http`/`https` 之外的链接协议在渲染侧被丢弃。字段名沿用 `notes`，不改。
_Avoid_：更新日志、更新说明、release notes（都指同一件东西，文档统一叫「变更说明」）

## 开机自启

设置「通用」中的全局开关，开启后跟随系统登录自动运行程序。静默启动从属于它：静默启动只修饰自启的那一次启动，关闭自启时静默启动一并复位为关。
_Avoid_：自动启动、自启动、后台运行（后台驻留是程序既有行为——关主窗口即驻留托盘——与是否自启无关）

## 静默启动

开机自启的子选项：由自启启动的那一次运行不显示主窗口，仅在系统托盘驻留。只作用于自启路径——手动启动、更新后重启、唤起已运行的实例都照常显示主窗口（见 ADR-0015）。
_Avoid_：后台启动、隐藏启动、无窗口启动

## 安装版

通过安装器部署的发布构建，是唯一注册开机自启的运行时身份，与开发实例的运行身份、数据和生命周期完全隔离（见 ADR-0018）。
_Avoid_：正式版、release 版（口头可以，文档统一叫「安装版」）

## 开发实例

开发过程中运行的构建（`pnpm tauri:dev` 启动）：拥有独立的运行身份与数据（首次启动为空库），窗口标题带 (dev) 标记，永不注册开机自启，可与安装版并存运行、互不干扰。
_Avoid_：测试版、debug 版、本地版
